{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Karma Attach",
            "type": "chrome",
            "request": "launch",
            "sourceMaps": true,
            "port": 9222,
            "url": "http://localhost:9876/debug.html",
            "webRoot": "${workspaceRoot}",
            "pathMapping": {
                "/": "${workspaceFolder}",
                "/base/": "${workspaceFolder}"
            }
        }
    ]
}